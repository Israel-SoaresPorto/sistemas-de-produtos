<?php

$host = 'localhost';
$user = 'root';
$password = '';
$database = 'produtos';

$conn = new mysqli($host, $user, $password, $database);

/* if($conn) {
    echo "OK";
} else {
    echo $conn->error();
} */
